module Main where

import Math
import Text.Printf

--import System.IO

main :: IO ()
main = do
    putStrLn "%!PS-Adobe-2.0 EPSF-2.0"
    putStrLn (matheval (Add (Num 2) (Num 4)))
    
    return ()
    
    {-
    writeFile "result.ps" contents
        where contents = matheval (Add (Num 2) (Num 4))
-}
     
    --putStrLn "%!PS-Adobe-2.0 EPSF-2.0" ++ putStrLn $ matheval (Add (Num 2) (Num 4))

